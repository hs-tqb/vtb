module.exports = ({query,store})=>{
  // if ( query.lang ) {
  //   store.commit('setLanguage', query.lang)
  // }
  // if ( query.ref_code ) {
  //   store.commit('setRefCode', query.ref_code)
  // }
}